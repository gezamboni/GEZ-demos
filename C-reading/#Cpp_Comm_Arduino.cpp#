#include <termios.h>

// definicao da device
const char *device = "/dev/tty96B0";

//return file handle para a device
O_RDWR | O_NOCTTY | O_NDELAY);
if (fd == -1) {
  printf("falha ao abrir a porta\n");
 }